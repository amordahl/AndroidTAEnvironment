import argparse
parser = argparse.ArgumentParser("Helps filter out flows based on the precision of "
"the configuration that generateed it. If a flow is generated by a configuration and then"
"not generated in a configuration that is strictly more precise than it, then it will"
"be filtered out.")


import logging
logging.basicConfig(level=logging.DEBUG)

import xml.etree.ElementTree as ET
d
logging.debug(f"Opening {args.flows}")
flows_root = ET.parse(args.flows).getroot()
for flow in flows_root:
    generating_config = flow.get("generating_config")
    
